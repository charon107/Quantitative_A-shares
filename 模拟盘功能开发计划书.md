# 模拟盘功能开发计划书

> 版本：v1.0（草案）｜撰写日期：2026-07-27｜撰写人：产品  
> 状态：待评审 ｜ 目标读者：后端 / 前端 / 测试 / 数据开发

---

## 1. 背景与目标

### 1.1 背景

WechatNum 目前已具备 A 股前复权日线行情看板能力（大盘概览、个股 K 线、排行榜、多头时长统计等）。用户在看盘、发现标的后，缺少一个**零风险的买卖演练与策略验证闭环**。模拟盘功能用于补齐「看盘 → 下单 → 持仓 → 复盘」的完整链路，提升用户留存与使用深度。

### 1.2 产品目标

- 让用户可以基于站内行情进行模拟买卖，形成持仓与收益曲线；
- 让用户可以对比自己的模拟收益与市场基准（等权指数），验证选股/择时能力；
- 为后续「策略回测」「组合分享」等高级功能沉淀账户、撮合、收益计算三套基础能力。

### 1.3 成功度量（北极星与过程指标）

| 指标      | 口径                 | 目标（上线后 1 个月） |
| ------- | ------------------ | ------------ |
| 模拟盘激活率  | 创建模拟账户的用户 / 访问用户   | ≥ 30%        |
| 首单转化率   | 完成首笔委托的用户 / 创建账户用户 | ≥ 60%        |
| 周活跃交易用户 | 每周有 ≥1 笔委托或查看持仓的用户 | 持续增长         |
| 数据正确性   | 对账差异（市值/现金/收益）     | 0 笔错误投诉      |

---

## 2. 关键约束（开发前必读）

以下约束源于现有架构，**直接决定技术方案选型**：

1. **行情数据为日频**：现有 DuckDB 仅存前复权日线，无盘中实时/分钟数据。因此模拟盘**不是实时撮合**，成交只能发生在交易日收盘价（或下一交易日开盘价），必须在产品文案中向用户明确披露。
2. **market.duckdb 每日原子替换**：行情库每天由入库脚本整体替换（见 `src/db.py` 原子替换机制），**模拟盘业务数据（账户/委托/持仓/资金）严禁写入 market.duckdb**，必须使用独立的存储文件。
3. **前复权价格偏差**：前复权价在分红除息后会发生历史价格回溯调整，长期持仓的市值会与真实世界有偏差。MVP 阶段接受该偏差并在 UI 披露；后续版本评估引入不复权价 + 分红处理。
4. **现有技术栈**：后端 FastAPI（`src/api/`）、计算层 `src/metrics.py`、缓存 Redis L2（可优雅降级）、前端 React + TS + Tailwind + ECharts，前端产物 `dist/` 由 FastAPI 同进程托管。模拟盘应**复用同一套技术栈与设计风格**（DESIGN.md：Anthropic 浅色暖调、红涨绿跌）。
5. **服务器资源有限**：小内存服务器，避免引入重型中间件；模拟盘存储建议使用独立 DuckDB 文件（`paper.duckdb`）或 SQLite，单文件、零常驻。
6. **无用户系统**：当前站点无登录体系。MVP 阶段采用「本地单账户」方案（见 4.1），避免引入注册登录的复杂度。

---

## 3. 需求范围

### 3.1 In Scope（本期）

- 模拟账户创建、重置、初始资金设定
- 股票买入 / 卖出委托（限价、市价）
- 基于收盘价的日频撮合引擎（含 T+1、涨跌停、整手、费用规则）
- 持仓、可用资金、冻结资金、市值、浮动盈亏
- 委托记录、成交记录、资金流水
- 账户收益分析：累计收益曲线、收益率、最大回撤、与等权指数基准对比
- 前端「模拟盘」新页面（Dashboard + 下单面板 + 2记录查询）

### 3.2 Out of Scope（本期不做，预留扩展）

- 多账户 / 多人排行榜（需用户体系，P3）
- 盘中实时撮合、Level-2（无数据源）
- 融资融券、做空、期权、基金/ETF 申赎
- 分红送股、拆股的精确公司行动处理
- 真实券商对接、实盘交易
- 策略自动化（条件单、网格、定投）

### 3.3 优先级定义

- **P0**：没有就无法形成最小闭环，本期必须交付
- **P1**：显著提升体验，本期尽量交付，可降级
- **P2**：锦上添花，可放入下期

---

## 4. 功能需求详述

### 4.1 模拟账户（P0）

- 用户首次进入模拟盘页面时，引导创建账户：设置账户名称、初始资金（默认 100 万元，可选 10万/50万/100万/500万/自定义）。
- MVP 采用**浏览器本地身份标识**（localStorage 生成 UUID 作为 account_id），同一浏览器即同一账户；无需登录。账户 ID 支持用户自行备份/导入（复制字符串），降低换浏览器丢账户的投诉风险。（P1）
- 支持「重置账户」：清空全部持仓/委托/资金，恢复到初始资金。重置为高危操作，需二次确认，且**保留重置前快照**（P1，便于找回与对账）。
- 一个浏览器仅一个活跃账户（MVP 不做多账户）。

### 4.2 交易下单（P0）

**入口**：模拟盘主页下单面板；个股查询页增加「模拟买入」快捷入口（P1）。

**下单要素**：股票（搜索选择，复用 `/api/stocks/search`）、方向（买入/卖出）、价格类型（市价/限价）、价格（限价时必填）、数量（股，买入须为 100 的整数倍；卖出可零股一次性清仓——MVP 简化为卖出也须整手，零股不允许产生）。

**校验规则（前端 + 后端双重校验）**：

- 仅支持沪深 A 股普通股票；停牌（当日无行情）禁止下单；
- 买入金额（含预估费用）≤ 可用资金；卖出数量 ≤ 可卖数量（T+1：当日买入不可卖）；
- 限价单价格合法性：不得超出当日涨跌停范围（见 4.3）；
- 数量合法性：100 股整数倍，单笔 ≤ 100 万股；
- 非交易日下单：接受委托，进入「待成交」队列，于下一交易日撮合。

**撤单（P1）**：未成交的限价单可在撮合前撤销，解冻资金/股票。

### 4.3 撮合引擎（P0，本功能的心脏）

**撮合时机**：每日行情入库完成后（`load_all_parquet.py` 之后），由脚本/任务触发对全部「待成交」委托批量撮合。撮合结果写入库并可触发 Redis 缓存清理。

**撮合规则（日频，收盘价模式）**：

| 场景               | 成交规则                                         |
| ---------------- | -------------------------------------------- |
| 市价单（盘中提交，T 日有行情） | 按 **T 日收盘价**成交                               |
| 市价单（收盘后/非交易日提交）  | 按**下一交易日收盘价**成交                              |
| 限价买单             | 若收盘价 ≤ 限价，按**收盘价**成交；否则不成交，继续挂账（有效期见下）       |
| 限价卖单             | 若收盘价 ≥ 限价，按**收盘价**成交；否则不成交，继续挂账              |
| 涨跌停              | 涨停（收盘价 = 涨停价）时买单**不成交**（模拟买不进）；跌停时卖单**不成交**  |
| 委托有效期            | 限价单默认 T+5 个交易日内有效，过期自动作废并解冻（P1；MVP 可简化为当日有效） |

**交易规则（A 股标准）**：

- **T+1**：当日买入的股票次一交易日方可卖出；
- **涨跌停幅度**：主板 ±10%，创业板（300xxx）/科创板（688xxx）±20%，ST/*ST ±5%（科创板 ST 仍 ±20%），北交所（8xxxxx/4xxxxx）±30%——**依赖股票板块与 ST 状态元数据，需确认现有库是否具备，若无则新增静态维表**（见 7.3 开放问题）；
- **整手**：买入 100 股整数倍；
- **新股上市首日**：无涨跌幅限制的上市首日（如注册制新股前 5 日）MVP 简化为按收盘价正常撮合，不做特殊限制，UI 不特殊提示。

**费用模型（P0，参数化配置，不写死）**：

| 费用  | 方向       | 费率（默认）       | 说明           |
| --- | -------- | ------------ | ------------ |
| 佣金  | 买卖双向     | 万 2.5，最低 5 元 | 可配置          |
| 印花税 | 卖出       | 0.05%        | 按现行税率，可配置    |
| 过户费 | 买卖双向（沪市） | 0.001%       | P1，MVP 可并入佣金 |

> 撮合规则与费率全部落在独立配置/常量模块，便于后续调整与单测。**必须提供「撮合回放」能力**：给定任意历史交易日，可重算当日应成交委托，供测试与对账。

### 4.4 持仓与资金（P0）

- **资金三态**：可用资金、冻结资金（未成交买单占用）、（市值单列）。`总资产 = 可用 + 冻结 + 持仓市值`。
- **持仓字段**：股票代码/名称、持仓数量、可卖数量（T+1 后）、成本价（摊薄）、最新收盘价、市值、浮动盈亏（金额与 %）、当日盈亏（P1）。
- **成本计算**：买入摊薄成本法（移动加权平均）；卖出按摊薄成本结转实现盈亏。
- **估值频率**：持仓市值随每日行情入库后更新（收盘价估值）；无盘中估值。

### 4.5 记录与流水（P0）

- **委托记录**：全部委托（含未成交/已成交/已撤销/已作废）及状态流转时间。
- **成交记录**：每笔成交的价格、数量、费用、金额。
- **资金流水**：每笔资金变动（冻结/解冻/买入扣款/卖出到账/重置）及变动后余额，**用于对账，必须可回溯**。
- 前端提供按日期/股票/状态筛选与分页。

### 4.6 收益分析（P0 基础版 / P1 增强版）

- **P0**：账户净值曲线（每日总资产，ECharts 面积图）、累计收益率、与等权指数基准对比（复用 `/api/market/equal-weight-index`）、期末总资产/总盈亏 KPI。
- **P1**：年化收益率、最大回撤、夏普比率、胜率（盈利卖出笔数/总卖出笔数）、个股盈亏贡献排行。
- 净值曲线从账户创建日起算，基准做同区间归一化（起点 = 1）。

### 4.7 前端页面（P0）

新增顶部导航「模拟盘」，遵循 DESIGN.md 设计系统（浅色暖调、陶土强调、红涨绿跌、等宽数字）。页面结构：

1. **账户总览区**：KpiCard × 4（总资产 / 可用资金 / 持仓市值 / 累计盈亏，含当日 delta——P1）；
2. **净值曲线区**：累计收益 vs 等权指数（范围 1M/3M/6M/全部，复用现有图表交互）；
3. **持仓列表**：表格（代码/名称/数量/可卖/成本/现价/市值/盈亏%），行内快捷「卖出」；
4. **下单面板**：搜索选股、买卖方向切换、市价/限价、数量（含 1/4、1/3、1/2、全仓快捷键——P1）、预估金额与费用、可用资金提示；
5. **记录区 Tab**：委托 / 成交 / 资金流水，支持筛选分页；
6. **风险披露**：固定文案位（见 9. 合规）。

---

## 5. 技术方案（指导方向，细节由开发设计评审确定）

### 5.1 总体架构

```
┌─ React SPA ── 新增 PaperTrade 页面 ─┐
│        │ JSON /api/paper/*          │
├─ FastAPI ─ src/api/routes/paper.py ─┤  新增路由模块
│        │                            │
│  src/paper_trading/                 │  新增业务模块（账户/撮合/资金/收益）
│        │                            │
│  paper.duckdb (独立单文件, 可写) ───┤  模拟盘业务数据
│                                     │
│  market.duckdb (只读, 每日替换) ────┘  行情数据（撮合取价/估值，只读依赖）
```

**核心决策**：

1. **独立存储 `paper.duckdb`**：账户、委托、成交、资金流水、净值快照全部落独立文件，与每日被原子替换的 `market.duckdb` 完全解耦；备份策略与行情库分离（deploy 备份脚本需同步纳入——P1）。
2. **撮合作为独立纯函数模块** `src/paper_trading/matcher.py`：输入（待成交委托 + 当日行情 + 配置）→ 输出（成交单 + 资金变动），不依赖 FastAPI，便于 pytest 全量覆盖与历史回放。
3. **撮合触发**：日更入库脚本执行完毕后调用撮合 CLI（`scripts/paper_match_daily.py`），纳入现有每日流水线（GitHub Actions → 服务器 load → 撮合 → 清缓存）。
4. **前端**：`frontend/src/pages/PaperTrade/`，复用 KpiCard / SearchBox / 表格 / ECharts 主题，新增路由与导航项。
5. **写接口不做 Redis 缓存**；净值曲线等读接口可复用 L2 缓存（键含 account_id，撮合后失效）。

### 5.2 API 草案（/api/paper/*）

| 方法     | 路径                                             | 说明                    | 优先级 |
| ------ | ---------------------------------------------- | --------------------- | --- |
| POST   | `/api/paper/accounts`                          | 创建账户（名称、初始资金）         | P0  |
| GET    | `/api/paper/accounts/{id}/overview`            | 账户总览（总资产/可用/冻结/市值/盈亏） | P0  |
| POST   | `/api/paper/accounts/{id}/reset`               | 重置账户（二次确认）            | P0  |
| GET    | `/api/paper/accounts/{id}/positions`           | 持仓列表                  | P0  |
| POST   | `/api/paper/accounts/{id}/orders`              | 提交委托（方向/价格类型/数量）      | P0  |
| DELETE | `/api/paper/accounts/{id}/orders/{order_id}`   | 撤单                    | P1  |
| GET    | `/api/paper/accounts/{id}/orders?status=`      | 委托记录（筛选/分页）           | P0  |
| GET    | `/api/paper/accounts/{id}/fills`               | 成交记录                  | P0  |
| GET    | `/api/paper/accounts/{id}/cash-flows`          | 资金流水                  | P0  |
| GET    | `/api/paper/accounts/{id}/equity-curve?start=` | 净值曲线 + 基准对比           | P0  |
| GET    | `/api/paper/accounts/{id}/metrics`             | 年化/回撤/夏普/胜率           | P1  |

约定：账户 ID 经请求头 `X-Paper-Account: <uuid>` 传递，后端校验归属；所有写操作幂等（客户端生成 request_id 防重复提交）。

### 5.3 数据表草案（paper.duckdb）

| 表                  | 关键字段                                                                                                                        | 说明            |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------- | ------------- |
| `accounts`         | account_id, name, init_cash, created_at, status                                                                             | 账户            |
| `orders`           | order_id, account_id, code, side, price_type, limit_price, qty, status(待成交/已成交/已撤销/已作废), request_id, created_at, updated_at | 委托            |
| `fills`            | fill_id, order_id, code, side, price, qty, fee, tax, amount, trade_date                                                     | 成交            |
| `positions`        | account_id, code, qty, sellable_qty, cost_price, updated_at                                                                 | 持仓（实时态）       |
| `cash_flows`       | flow_id, account_id, type(冻结/解冻/买入/卖出/重置), amount, balance_after, ref_id, created_at                                        | 资金流水（对账依据）    |
| `equity_snapshots` | account_id, trade_date, cash, frozen, market_value, total_asset                                                             | 每日净值快照（撮合后生成） |

> 字段与约束以开发详设为准；状态机（委托状态流转）需在详设中画出并评审。

### 5.4 与现有模块的复用关系

- 行情取价：`src/db.py` 只读连接，撮合按 code + trade_date 取收盘价/涨跌停判断所需字段；
- 基准对比：复用 `metrics.py` 等权指数计算；
- 前端组件：KpiCard / SearchBox / ECharts 主题 / 表格样式直接复用。

---

## 6. 里程碑与排期（建议）

| 阶段      | 内容                          | 产出                     | 建议周期  |
| ------- | --------------------------- | ---------------------- | ----- |
| M0 评审   | 本计划书评审；详设（表结构/状态机/API 契约）   | 详设文档 + 接口契约            | 3 天   |
| M1 后端核心 | 存储层 + 账户/下单/撮合纯函数 + 单测      | 可独立运行的撮合模块 + pytest 覆盖 | 1.5 周 |
| M2 后端接口 | /api/paper/* 路由 + 每日撮合流水线接入 | API 可用（Swagger 自测通过）   | 1 周   |
| M3 前端页面 | 模拟盘整页（总览/持仓/下单/记录/净值）       | 可联调的前端页面               | 1.5 周 |
| M4 联调测试 | 端到端用例、对账验证、撮合历史回放验证         | 测试报告 + 修复              | 1 周   |
| M5 灰度上线 | 部署、备份纳入、文案披露检查、上线观察         | 上线公告                   | 3 天   |

> 全周期约 5 周（1 后端 + 1 前端 + 0.5 测试人力估算）。P1 项按进度裁剪，裁剪需产品确认。

## 7. 测试计划（要点）

1. **撮合单元测试（核心）**：市价/限价/涨跌停不成交/停牌不成交/T+1 不可卖/整手校验/最低佣金/印花税方向/委托过期——每个规则独立用例；
2. **资金一致性（对账）**：任意时间点 `可用 + 冻结 = 初始资金 − Σ买入 + Σ卖出 − Σ费用`，且与资金流水逐笔核对；测试期间每日自动跑对账脚本；
3. **历史回放测试**：选取 20 只典型股票（含 ST/创业板/科创板/停牌股）近 1 年行情，模拟固定委托序列回放，结果与人工核算样例比对；
4. **API 测试**：TestClient 覆盖全部路由的正常/边界/并发重复提交（request_id 幂等）；
5. **前端 E2E（P1）**：下单→撮合→持仓更新→收益曲线全链路；
6. **性能**：1 万笔待成交委托的单日撮合耗时应 < 30s（DuckDB 向量化取价）。

## 8. 风险与应对

| 风险                     | 影响               | 应对                             |
| ---------------------- | ---------------- | ------------------------------ |
| 前复权价导致长期持仓市值偏差         | 收益数字与真实世界不符，用户质疑 | UI 显著披露；P3 评估引入不复权价 + 分红处理     |
| 日频撮合被用户误解为实时交易         | 投诉/认知偏差          | 下单页、成交记录、帮助文案三处明确「按收盘价成交」      |
| 股票板块/ST 元数据缺失导致涨跌停规则错误 | 撮合错误             | M0 阶段确认数据可得性，缺失则新增静态维表并随日更维护   |
| paper.duckdb 损坏/未备份    | 用户资产数据丢失         | 纳入每日备份（deploy 脚本）；重置保留快照       |
| 本地账户 ID 丢失（清缓存/换浏览器）   | 用户找不到账户          | 账户 ID 导出/导入功能（P1）；文案提示备份       |
| 每日撮合任务失败未告警            | 委托长期不成交          | 撮合纳入流水线监控，失败告警 + 可手动补跑（按交易日回放） |

## 9. 合规与文案（必须）

- 页面固定披露：「本功能为模拟交易，不构成任何投资建议；成交按收盘价撮合，与真实交易存在差异；历史行情为前复权数据，长期持仓收益可能与真实情况存在偏差。」
- 全站既有免责声明（README：仅供量化研究）同步覆盖模拟盘页面。

## 10. 开放问题（M0 评审需闭环）

1. 股票板块（沪/深/创/科/北）与 ST 状态元数据当前库中是否已有？若无，维表来源与更新机制？
2. 撮合价采用「收盘价」还是「次日开盘价」模式——本计划默认收盘价（实现简单、可当日出结果），是否有异议？
3. 是否需要支持同一浏览器多账户/账户切换？（MVP 默认不做）
4. 账户 ID 的备份/导入交互形态（二维码 or 字符串复制）？
5. 委托有效期：MVP 是否接受「限价单当日有效」的简化？
6. 排行榜（多账户收益 PK）是否列入下一期 OKR？

---

## 11. 评审记录（2026-07-27，对照现有代码库逐项核实）

**结论：计划书通过评审，可进入开发。** 核心假设（日频行情、market.duckdb 原子替换、独立 paper.duckdb、等权指数可复用、技术栈）均核实属实。以下为核实中发现的偏差及 M0 开放问题的闭环决议：

### 11.1 与现有代码的偏差（开发按此修正执行）

1. 撮合取价无现成函数：库中日线表为 `kline(code,date,open,high,low,close,volume,amount,pctChg,turn)`，PK `(code,date)`，需新写一条按 code+date 的 PK 查询（`src/db.py` 只读连接）。
2. CORS 仅允许 GET（`src/api/main.py`）：需为 `/api/paper/*` 写接口放宽 POST/DELETE；前端 `api/client.ts` 仅有 GET 封装，需新增 mutation 封装。
3. 无入库后钩子：撮合接入点选在 `.github/workflows/daily_ingest.yml` 的 ssh 脚本中 `load_all_parquet.py` 之后追加 `scripts/paper_match_daily.py`（零侵入，与 warmup 同风格）。
4. Redis 缓存仅支持 `invalidate_all()` 全清：撮合后沿用现有惯例全清，不新增单键失效 API。
5. 前端无 react-router：新页面为 `frontend/src/pages/PaperTrade.tsx` 单文件，在 `App.tsx` 的 `PAGES` 数组注册。
6. pytest 无配置块，测试沿用 `tests/conftest.py` 的临时 DuckDB fixture 模式，paper 库测试仿照新建。

### 11.2 开放问题闭环（第 10 节）

1. **板块/ST 元数据**：库中 `stock_info` 为空、无独立板块维表，且**数据集中不存在北交所股票**（采集仅覆盖沪深）。决议：MVP 不新建维表，涨跌停规则按 code 前缀推断（`sh.6*`/`sz.0*` 主板 ±10%；`sz.300/301` 创业板 ±20%；`sh.688/689` 科创板 ±20%），ST 状态按 `stock_meta.code_name` 当前名称含 "ST" 推断（主板 ST ±5%；科创板 ST 仍 ±20%）；北交所 ±30% 规则留空实现。历史 ST 状态不可知，属已知偏差，随前复权偏差一并披露。
2. **撮合价模式**：确认采用收盘价模式（实现简单、当日入库后即出结果）。
3. **多账户**：MVP 不做，一浏览器一账户。
4. **账户 ID 备份**：字符串复制（不做二维码）。
5. **委托有效期**：MVP 限价单当日有效，当日未成交即作废解冻。
6. **排行榜**：不列入本期。

### 11.3 评审中补充的关键设计决策

- **可卖数量（T+1）**：撮合仅在每日收盘后批量执行，买单成交于 X 日收盘、卖单最早成交于 >X 日收盘，T+1 天然满足。下单校验按 `可卖 = 持仓 qty − 该 code 未成交卖单合计` 计算，无需单独维护 sellable 快照列。
- **卖出冻结**：卖出委托提交即按上式扣减可卖，撤单/作废自动恢复，无需额外冻结列。
- **账户凭证**：path 中的 account_id（UUID）即能力凭证，省略 `X-Paper-Account` 请求头，避免双通道不一致；所有写接口幂等靠 `(account_id, request_id)` 唯一约束。
- **写库并发**：API 对 paper.duckdb 采用短连接（开→写→关），撮合 CLI 同样短连接 + 锁冲突重试，避免与常驻 API 进程的 DuckDB 单写者锁冲突。
- **过户费**：MVP 并入佣金，配置模块预留参数位。
- **重置快照**：重置前将账户总览写入 `account_resets` 表后清空业务数据，满足 P1 快照要求。

---

> 本文档为开发依据，评审通过后进入详设；范围变更需产品书面确认。
